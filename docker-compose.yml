services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      # Persist SQLite DB (this app uses .data, not data/)
      - joltcsv-data:/app/.data
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  joltcsv-data:
